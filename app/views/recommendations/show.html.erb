<h1>Item #<%= @item.id %></h1>

<p>
  <%= link_to "Go Back", recommendations_url %>
</p>
<div class="recommendation">
    <%= link_to image_tag(@item.image_url), recommendation_url(@item.id) %>
  <h1>
    <%= link_to @item.title, recommendation_url(@item.id) %>
      <% if @item.free? %>
        <%= image_tag 'free.jpg', class: 'free' %>
      <% end %>
  </h1>
  <p><%= @item.description %></p>
  <p>
    <%= link_to "Delete", recommendation_url(@item.id), method: 'delete' %>
    <%= link_to "Edit", edit_recommendation_url(@item.id) %>
  </p>
</div>



