<h1>I Recommend...</h1>

<ul>
  <% item_number = 0 %>
  <% @recommendations.each do |r| %>
    <li>
      <%= link_to "#{r[:title]}: #{r[:description]}", details_url(item_number) %>
    </li>

    <% item_number = item_number + 1 %>
  <% end %>
</ul>
