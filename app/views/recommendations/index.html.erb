<h1>I Recommend...</h1>

<ul>
  <% @recommendations.each do |r| %>
    <li>
      <%= link_to "#{r.title}: #{r.description}", details_url(r.id) %>
      <%= link_to "Toast", "/delete/#{r.id}" %>
    </li>
  <% end %>
</ul>
